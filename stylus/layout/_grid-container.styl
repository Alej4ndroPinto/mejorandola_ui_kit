// Grid container

.grid
  @extends $clearfix
  @extend .centered 
  width: 90%

  // Make nested grid 100%

  [class*="grid__column--"] > & //reverse nesting
    width: 100%

  +media-query('large')
    max-width: grid-container-width
